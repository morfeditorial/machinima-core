<!doctype html>
<html lang="uk">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, shrink-to-fit=no" />
    <title>{% if platform_context().isEmbedded %}Morf{% else %}Machinima{% endif %}</title>
    <meta name="turbo-cache-control" content="no-cache" />

    <script>
        window.__PLATFORM__ = {
            isEmbedded: {{ platform_context().isEmbedded ? 'true' : 'false' }},
            platformName: '{{ platform_context().platformName }}',
            theme: '{{ platform_context().theme }}',

            capabilities: {{ platform_context().capabilities|json_encode|raw }}
        };
        // Every registered adapter's bootstrap module (empty once a session is
        // authenticated — see PlatformUiContextRuntime::getBootstrapModulePaths()).

        if (window.__PLATFORM__.isEmbedded) {
            document.title = 'Morf TMA';
        }
    </script>
    <meta name="platform-bootstrap-module-paths" content="{{ platform_bootstrap_module_paths()|json_encode|e('html_attr') }}">
    {% if platform_ui_hints_module_path() %}
        <meta name="platform-ui-hints-module-path" content="{{ platform_ui_hints_module_path() }}">
    {% endif %}
    <link rel="stylesheet" href="{{ asset('bundles/machinimacore/css/machinima_core.css') }}?v={{ random() }}">
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body data-route="{{ app.request.attributes.get('_route') }}">
    {% for splash in ui_splash_screens() %}
        <template id="splash-template-{{ splash.platformName }}">
            {% include splash.templatePath ignore missing %}
        </template>
        {% if splash.displayCondition %}
            <script data-turbo-eval="false">
                if (!window.__splashShown_{{ splash.platformName }}) {
                    window.__splashShown_{{ splash.platformName }} = true;
                    if ({{ splash.displayCondition|raw }}) {
                        var t = document.getElementById('splash-template-{{ splash.platformName }}');
                        if (t) {
                            document.currentScript.parentNode.insertBefore(t.content.cloneNode(true), document.currentScript);
                        }
                    }
                }
            </script>
        {% endif %}
    {% endfor %}

    <div id="not-supported" style="display:none">
        <div class="w-full max-w-600 mx-auto p-4 pb-0 min-h-screen flex items-center justify-center">
            <div class="text-center py-8 px-4 mb-8 bg-surface rounded border-2 border-border shadow-brutal w-full">
                <div class="w-22 h-22 mx-auto mb-6 rounded bg-surface flex items-center justify-center text-primary border-2 border-border shadow-brutal overflow-hidden shrink-0">
                    <i data-lucide="lock" class="w-10 h-10 stroke-1.5"></i>
                </div>
                <h1 class="text-2xl font-bold mb-3 m-0 font-unbounded tracking-tight uppercase">Machinima</h1>
                <p class="text-base text-primary mb-6 leading-relaxed font-semibold">
                    Цей застосунок призначено для використання в підтримуваному середовищі.
                </p>
                <a href="{{ platform_context().botLink ?? '#' }}" id="not-supported-bot-link" class="bg-border text-surface py-3 px-6 rounded font-bold text-sm uppercase flex items-center justify-center cursor-pointer border-2 border-border shadow-btn transition-all font-unbounded active:translate-brutal-sm active:shadow-xs w-full">
                    <i data-lucide="external-link" class="mr-2 w-5 h-5 stroke-2.5"></i>Відкрити застосунок
                </a>
            </div>
        </div>
    </div>
    <main id="main-content" class="turbo-transition">
        {% block body %}{% endblock %}
    </main>

    {% block tabbar %}
    {% set current_route = app.request.attributes.get('_route') %}
    <nav id="main-tab-bar" data-turbo-permanent class="fixed bottom-0 left-0 right-0 bg-surface border-t-2 border-border flex safe-pb z-100 max-w-600 mx-auto">
        {% set active_classes = 'opacity-100 font-extrabold bg-black-5 border-t-3 border-border -mt-0.5' %}
        {% set default_classes = 'opacity-60 font-bold' %}

        <a href="{{ path('app_index') }}" data-skeleton="feed" class="flex-1 flex flex-col items-center justify-center py-4 text-primary text-xs transition-all uppercase font-unbounded active:bg-black-10 {% if current_route == 'app_index' %}{{ active_classes }}{% else %}{{ default_classes }}{% endif %}">
            <i data-lucide="play-square" class="mb-1 w-6 h-6 stroke-2.5"></i>
            <span>Стрічка</span>
        </a>
        <a href="{{ path('app_categories') }}" data-skeleton="categories" class="flex-1 flex flex-col items-center justify-center py-4 text-primary text-xs transition-all uppercase font-unbounded active:bg-black-10 {% if current_route == 'app_categories' %}{{ active_classes }}{% else %}{{ default_classes }}{% endif %}">
            <i data-lucide="layout-grid" class="mb-1 w-6 h-6 stroke-2.5"></i>
            <span>Категорії</span>
        </a>
        <a href="{{ path('app_authors') }}" data-skeleton="authors" class="flex-1 flex flex-col items-center justify-center py-4 text-primary text-xs transition-all uppercase font-unbounded active:bg-black-10 {% if current_route == 'app_authors' %}{{ active_classes }}{% else %}{{ default_classes }}{% endif %}">
            <i data-lucide="users" class="mb-1 w-6 h-6 stroke-2.5"></i>
            <span>Автори</span>
        </a>
        <a href="{{ path('app_notifications') }}" data-skeleton="notifications" class="flex-1 flex flex-col items-center justify-center py-4 text-primary text-xs transition-all uppercase font-unbounded active:bg-black-10 {% if current_route == 'app_notifications' %}{{ active_classes }}{% else %}{{ default_classes }}{% endif %}">
            <div class="relative mb-1 w-6 h-6 flex items-center justify-center">
                <i data-lucide="bell" class="w-6 h-6 stroke-2.5"></i>
                <span id="unread-count-badge" class="hidden absolute top-0 right-0 bg-accent text-white font-unbounded text-xs font-bold py-0.5 px-1 rounded leading-none">0</span>
            </div>
            <span>Сповіщ.</span>
        </a>
        <a href="{{ path('app_profile') }}" data-skeleton="profile" class="flex-1 flex flex-col items-center justify-center py-4 text-primary text-xs transition-all uppercase font-unbounded active:bg-black-10 {% if current_route == 'app_profile' %}{{ active_classes }}{% else %}{{ default_classes }}{% endif %}">
            <i data-lucide="user" class="mb-1 w-6 h-6 stroke-2.5"></i>
            <span>Профіль</span>
        </a>
    </nav>
    {% endblock %}

    <script type="module" src="https://unpkg.com/@hotwired/turbo@8.0.4/dist/turbo.es2017-esm.js"></script>
    <script>
        window.APP_CONFIG = {
            mercureUrl: '{{ mercure("machinima/updates")|escape("js") }}'
        };

        if (!window.adjustPaddingInitialized) {
            window.adjustPaddingInitialized = true;
            let adjustPaddingFrame;
            window.adjustBottomPadding = function() {
                if (adjustPaddingFrame) cancelAnimationFrame(adjustPaddingFrame);

                adjustPaddingFrame = requestAnimationFrame(() => {
                    const bottomFixedElements = document.querySelectorAll('.fixed.bottom-0');
                    let maxPadding = 0;

                    bottomFixedElements.forEach(el => {
                        if (window.getComputedStyle(el).display !== 'none') {
                            const height = el.offsetHeight;
                            if (height > maxPadding) maxPadding = height;
                        }
                    });

                    document.documentElement.style.setProperty('--bottom-overlay-height', maxPadding + 'px');
                });
            };

            document.addEventListener('DOMContentLoaded', window.adjustBottomPadding);
            window.addEventListener('resize', window.adjustBottomPadding);
            document.addEventListener('turbo:load', window.adjustBottomPadding);
            document.addEventListener('turbo:render', window.adjustBottomPadding);

            const observer = new MutationObserver(window.adjustBottomPadding);
            observer.observe(document.documentElement, { childList: true, subtree: true });
        }
    </script>
    <script src="{{ asset('bundles/machinimacore/js/machinima_core.js') }}?v={{ random() }}"></script>
    {% include '@MachinimaCore/components/skeletons.html.twig' %}
    {% block javascripts %}{% endblock %}
</body>
</html>
